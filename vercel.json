{
  "buildCommand": "cd Asset-Manager && npm install && npx vite build",
  "outputDirectory": "Asset-Manager/dist/public",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}
